apps:
  - script: 'index.js'
    name: 'LANDING'
    watch: true
    # instances: 4
    # exec_mode: cluster
    env:
      PORT: 5000
      NODE_ENV: development
    env_production:
      PORT: 5000
      NODE_ENV: production
