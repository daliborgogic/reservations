apps:
  - script: 'index.js'
    name: 'RESERVE'
    watch: true
    # instances: 4
    # exec_mode: cluster
    env:
      PORT: 5001
      NODE_ENV: development
    env_production:
      PORT: 5001
      NODE_ENV: production
